## StepperOnline A6‑EC ESI files

Drop the vendor-provided ESI XML file(s) for the A6‑EC series drives into this folder.

Keep them **as-is** (no edits). The RT motion core will not parse these at runtime in production, but we
use them as an authoritative reference during bring-up and to document expected PDO/DC behavior.

