// Web UI constants mirroring the desktop UI, kept self-contained.
// Position jogging increments (millimeters)
export const POS_INCREMENT_OPTIONS_MM: number[] = [1, 5, 10, 50, 100];

// Orientation jogging increments (degrees)
export const ORI_INCREMENT_OPTIONS_DEG: number[] = [1, 5, 15, 45];

// Default slider starting value (0..1000) for speed multiplier
export const DEFAULT_SPEED_SLIDER = 500;


