# Temporary IgH config for binding the master to the RevPi EtherCAT NIC.
#
# Uses the NIC MAC so it stays valid even after interface renaming
# (eth0 -> ethercat0).
MASTER0_DEVICE="c8:3e:a7:14:1c:75"

DEVICE_MODULES="generic"

